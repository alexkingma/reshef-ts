import React from "react";
import { Zone } from "./Zone";

export const Counterweight = () => {
  // This component does nothing and renders nothing (visible).
  // Its only purpose is to assist with layout/zone alignment.
  return <Zone />;
};
